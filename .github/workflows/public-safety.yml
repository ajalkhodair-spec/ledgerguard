name: Public Safety

on:
  push:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Reject secrets, private paths, and unsupported claims
        run: bash scripts/check_public_safety.sh
